article.db
  h3
    a(title=JSON.stringify(db)) #{JSON.stringify(db)}
  p= JSON.stringify(db)

  //- .meta
  //-   span.muted= formatDate(db.createdAt)

  //-   - if (db.user)
  //-     | &nbsp; - &nbsp;
  //-     span Author &nbsp;

  //-     - var name = db.user.name ? db.user.name : db.user.username

  //-     a(href="/users/"+db.user._id)= name

  //-   - if (db.tags)
  //-     | &nbsp; - &nbsp;
  //-     span Tags &nbsp;
  //-     - each tag in db.tags.split(',')
  //-       i.muted.fa.fa-tag &nbsp;
  //-       a.tag(href="/tags/"+tag)= tag
  //-       | &nbsp;&nbsp;
